
#ativar UFW
sudo ufw enable

#desativar UFW
sudo ufw disable

#liberar porta
sudo ufw allow <porta ou Intervalo>

#negar conexao na porta
sudo ufw deny <porta ou Intervalo de porta>

#verificar status
sudo ufw status

#Arquivo de configuracao
sudo nano /etc/default/ufw

